@page "/map/events"

@inject NavigationManager _navigationManager



<div class="lf-map">
</div>
@code {
    /// <summary>
    /// Handles the button click event to navigate back to the events list page.
    /// This method provides an escape route from the not-yet-implemented map page.
    /// </summary>
    private void EventPage()
    {
        _navigationManager.NavigateTo("/events");
    }
}